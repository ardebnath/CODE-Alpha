This folder is where the built APK should be collected after Android Studio builds the project.

Expected debug APK path after a successful build:

`app/build/outputs/apk/debug/app-debug.apk`

No compiled APK is included yet because the Android build toolchain was not available on this machine during generation.
