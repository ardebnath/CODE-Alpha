# Alpha User Data

This folder stores local Alpha Studio browser accounts and saved projects.

Structure:

- `users/<username>/profile.json` stores the local account profile and password hash
- `users/<username>/projects/*.alpha` stores saved Alpha projects for that user
- `users/<username>/projects/*.json` stores project display names and metadata

Each user has a different password and a separate project folder.
