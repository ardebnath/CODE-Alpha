#include <stdio.h>

int main(void) {
    puts("Alpha native extension template");
    puts("Compile this file and connect it through a new bridge handler in Alpha.");
    puts("This shows how Alpha can grow into native-language integrations later.");
    return 0;
}
